# AGENTS.md — /api layer

You are working in the **backend layer — the contract owner**. Read this folder's [`README.md`](./README.md) first, then the root [`/CONTEXT.md`](../CONTEXT.md).

Pydantic models are the single source of truth. After changing a model, regenerate TS types into `/shared`. Read DuckDB per `../data/SCHEMA.md`. Stay in `/api`.
