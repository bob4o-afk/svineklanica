# docs/research — reference & background (NOT the active spec)

> The **active, authoritative** rules are `CLAUDE.md` + `.claude/rules/`. Everything here is **background, reference, and backup** from the team's pre-event exploration. Read it for context and reuse the pitch/rubric helpers — but if anything here contradicts `.claude/rules/`, the rules win.

## Pitch & judging (reuse these directly)
- **`05_pitch_playbook.md`** — 10-min pitch structure + the punk/"roast" angle, soundtrack notes.
- **`13_pitch_template.md`** — fill-in-the-blank pitch skeleton + Q&A index card.
- **`16_rubric_scorecard.md`** — the judging criteria as a live self-scoring checklist (Tech 30 / Radical 30 / UX 20 / Roast 20).
- **`17_readme_template.md`** — submission-day README template + checklist.

## Data (procurement project + general BG intel)
- **`02_data_sources.md`** — broader vetted Bulgarian data sources + verified access facts (WAF blocks, encodings, what's paywalled). Complements `.claude/rules/data-sources.md`.
- **`07_fact_check_verdicts.md`** — the idea fact-check + re-weighting that led here (why procurement, what got cut).

## Backup idea — election anomaly (if a track forces it)
- **`12_data_cheatsheet.md`** — CIK election-data schema cheat-sheet.
- **`14_anomaly_poc.md`** + **`14_fingerprint.png`** — a **working** Klimek "election fingerprint" POC on real 2021 CIK data. Ready fallback if the announced track pushes us toward elections instead of procurement.

## Superseded — earlier FastAPI/DuckDB stack (historical only)
- **`master_rules_OLD_STACK.md`**, **`agent_setup_OLD_STACK.md`** — engineering charter + agent setup from the **abandoned** Python/FastAPI/DuckDB plan. Kept only for the stack-agnostic process bits (git flow, Definition of Done, glossary idea). **The stack specifics are wrong now** — use `.claude/rules/` instead.
