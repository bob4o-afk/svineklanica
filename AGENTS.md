# AGENTS.md

**Read [`CONTEXT.md`](./CONTEXT.md) first — it is the single source of truth for this repo.** (Used by Codex, Copilot, and other agents that look for `AGENTS.md`.)

Then read the `README.md` of the layer you're working in (`/web`, `/api`, or `/data`) and, for anything non-trivial, `docs/master_rules.md`.

Do not duplicate guidance here — this file is a pointer so all AI tools read the same brain.
