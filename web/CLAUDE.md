# CLAUDE.md — /web layer

You are working in the **frontend layer**. Read this folder's [`README.md`](./README.md) first, then the root [`/CONTEXT.md`](../CONTEXT.md).

Stay in `/web`. Consume the API only via generated types in `/shared`. Bulgarian UI strings, English code.
