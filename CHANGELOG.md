# Changelog

One line per merge. Newest at the top. Keep it terse: `- <layer>: <what changed>`.

## Setup
- repo: environment shell ready — CONTEXT + per-tool pointers, /docs rule set, by-layer folders (web/api/data/shared), allowlist, .gitignore, README.
